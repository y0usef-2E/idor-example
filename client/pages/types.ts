// mainly for autocompletion (which explains the many `?` signs)
interface user_data {
  username?: string;
  id?: number;
  password?: string;
  address?: string;
  phone_number?: string;
  date_of_birth?: string;
  email_address?: string;
}
